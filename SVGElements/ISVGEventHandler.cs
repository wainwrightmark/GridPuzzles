namespace SVGElements;

public interface ISVGEventHandler
{
    string Name { get; }

    //void AddEventCallback(RenderTreeBuilder renderTreeBuilder, int i, object receiver);

    //EventCallback CreateEventCallback(object receiver);
}