@page "/Crossword"
@page "/Crossword/{Size:int}/{GridText}"
@page "/Crossword/{Size:int}"
@using GridPuzzles.Cells


<GridPage T="char"
          TCell="CharCell"
          Columns="@_size"
          Rows="@_size"
          GridCreator="CrosswordGridCreator.Instance"
          GetExtraFunc="CrosswordExport.ExportCrossword" 
          GridText="@GridText"/>


@code {

    [Parameter]
    public int Size { get; set; }

    private int _size => Size == default ? 15 : Size;

    [Parameter]
    public string? GridText { get; set; }
}