@page "/numbergrid/{Size:int}/{GridText}"
@page "/numbergrid/{Size:int}"
@using Sudoku
@using GridPuzzles.Cells

<GridPage T="int" 
          TCell="IntCell"
          Columns="@Size" 
          Rows="@Size" 
          GridCreator="SudokuGridCreator.Instance" 
          GridText="@GridText" 
          Examples="@Sudoku.Examples.ExampleHelper.SudokuExamples.ToList()"/>

@code {

    [Parameter]
    public int Size { get; set; }

    [Parameter]
    public string? GridText { get; set; }
}