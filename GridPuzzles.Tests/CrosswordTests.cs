namespace GridPuzzles.Tests;

/*
public class CrosswordTests
{
    private readonly ITestOutputHelper _output;

    public CrosswordTests(ITestOutputHelper output)
    {
        _output = output;
    }


    //[Fact]
    public void TestStandardCrossword()
    {
        var sw = Stopwatch.StartNew();
        var r =Solver.TrySolveAllCrossworduku(9, true);

        
        sw.Stop();
        _output.WriteLine(sw.ElapsedMilliseconds + " milliseconds");
        r.Should().NotBeNull();

        _output.WriteLine(r.Value.ToString());

        _output.WriteLine(r.Value.ToDisplayString());
    }

    //[Fact]
    public void TestMiniCrossword()
    {
        var sw = Stopwatch.StartNew();
        var r =Solver.TrySolveAllCrossworduku(6, true);

        
        sw.Stop();
        _output.WriteLine(sw.ElapsedMilliseconds + " milliseconds");
        r.Should().NotBeNull();

        _output.WriteLine(r.Value.ToString());

        _output.WriteLine(r.Value.ToDisplayString());
    }

}
*/